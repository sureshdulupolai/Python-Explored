"""
2️⃣ OpenCV

Webcam feed capture (cv2.VideoCapture)
Read/write images/videos (cv2.imread, cv2.imwrite)
Draw keypoints / annotations on frames (cv2.circle, cv2.line)
Resize/crop/rotate images
Convert color spaces (BGR → RGB / grayscale)

3️⃣ MediaPipe

Face landmarks (468 points)
Pose landmarks (33 points)
Hand landmarks if needed
Extract coordinates → send to backend for processing
"""