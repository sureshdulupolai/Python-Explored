📘 Git & GitHub Roadmap (Commands with Examples)

1️⃣ Basic Setup
----------------
git config --global user.name "Suresh"
git config --global user.email "suresh@example.com"
git config --list    # Verify configuration

2️⃣ Initialize & Clone
----------------------
git init                         # Start a new repo
git clone <repo_url>             # Clone from GitHub
git remote -v                    # Check remote URL

3️⃣ Basic Commands
------------------
git status                       # Check file status
git add .                        # Stage all changes
git commit -m "First commit"     # Commit changes
git log --oneline                # Show commit history

4️⃣ Branching
-------------
git branch                       # Show all branches
git branch <branch_name>         # Create new branch
git checkout <branch_name>       # Switch to branch
git checkout -b <branch_name>    # Create + Switch branch
git merge <branch_name>          # Merge into current branch
git branch -d <branch_name>      # Delete branch

5️⃣ Push & Pull
---------------
git remote add origin <repo_url> # Connect local to GitHub
git push origin main             # Push main branch
git push -u origin main          # First push with upstream
git pull origin main             # Pull updates from GitHub

6️⃣ Stash (Temporary Save)
---------------------------
git stash save "message"         # Save changes temporarily
git stash list                   # Show stash list
git stash apply stash@{0}        # Reapply stash
git stash drop stash@{0}         # Delete stash

7️⃣ Undo / Reset
----------------
git restore <file>               # Undo unstaged changes
git reset --hard HEAD            # Reset all changes
git reset <commit_id>            # Reset to specific commit
git revert <commit_id>           # Revert commit safely

8️⃣ Tags (Versioning)
---------------------
git tag v1.0                     # Create tag
git tag                          # List tags
git push origin v1.0             # Push tag to GitHub

9️⃣ GitHub Collaboration
-------------------------
git fetch origin                 # Fetch changes from GitHub
git pull origin main             # Update local branch
git push origin feature-branch   # Push feature branch
git merge feature-branch         # Merge into main

🔟 Advanced (Rebase, Cherry-pick)
---------------------------------
git rebase main                  # Reapply commits on main
git cherry-pick <commit_id>      # Pick specific commit

1️⃣1️⃣ Git Log Visualization
----------------------------
git log --oneline --graph --all

👉 Save this roadmap in a file 'git_roadmap.txt' and keep as reference.
